---
import photoAlbumJson from "../data/photo_album.json";
import PhotoCard from "./PhotoCard.astro";
import { formatDate } from "../utils/helper";

const thumbnailList = Object.values(photoAlbumJson.images).map(
    (image, index) => {
        return {
            index: index,
            src: `/images/photos/${image.name}.webp`,
            alt: image.name,
            dateFormattedString: formatDate(image.date),
            ...image,
        };
    },
);
---

<div id="photo-album-container">
    <div class="total">
        {photoAlbumJson.images.length} PHOTO{
            photoAlbumJson.images.length !== 1 ? "S" : ""
        }
    </div>
    <section id="photo-album-section">
        <div class="grid">
            {
                thumbnailList.map((thumbnail) => (
                    <PhotoCard
                        index={thumbnail.index}
                        name={thumbnail.name}
                        date={thumbnail.dateFormattedString}
                        characterList={thumbnail.characters}
                        tags={thumbnail.tags}
                        qrPostUrl={thumbnail.qrPostUrl}
                    />
                ))
            }
        </div>
        <!-- <div class="empty-state" id="empty-state">
            <div class="empty-state-icon">✦</div>
            <p>No images match the selected filters.</p>
        </div>-->
    </section>
</div>
